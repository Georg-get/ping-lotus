---
build docker:
  stage: build image
  image: docker:stable
  services:
    - docker:dind
  script:
    - docker login -u gitlab-ci-token -p ${CI_JOB_TOKEN} ${CI_REGISTRY}
    - docker build -t ${CI_REGISTRY}/$NAME_DOCKER_IMAGE . #
    - docker push ${CI_REGISTRY}/$NAME_DOCKER_IMAGE #
  #only:
    #- master
    #- merge_requests