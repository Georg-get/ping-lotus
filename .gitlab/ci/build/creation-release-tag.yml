---
creation release tag:
  stage: creation release tag
  script:
    #-
    - curl -s -X POST https://api.telegram.org/bot[$TELEGRAM_BOT_TOKEN]/sendMessage -d chat_id=[$TELEGRAM_GROUP_ID] -d text="Создан релизный тег ${}" # сообщение в телграм
  only:
    - master 