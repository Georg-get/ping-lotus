test ansible playbook job:
  image: ubuntu:18.04
  stage: test ansible playbook
  script:
    - apt update && apt install ansible
    - ansible-playbook -i ./infrastructure/ ansible.yml
    - curl localhost