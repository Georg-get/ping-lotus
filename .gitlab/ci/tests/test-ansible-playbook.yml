test ansible playbook job:
  image: ubuntu:18.04
  stage: test ansible playbook
  script:
    - apt update && apt install ansible -y
    - ansible-playbook ./infrastructure/ansible.yml
    - docker-compose -f docker-compose_test.yml up -d
    - curl localhost