telegram job:
  stage: telegram

  environment:
    url: $URL_TG

  script:
  - curl -s -X POST $URL_TG'${TELEGRAM_BOT_TOKEN}'/sendMessage -d chat_id='${TELEGRAM_GROUP_ID}' -d text="Джоба ${CI_JOB_ID},  ветки ${CI_COMMIT_BRANCH} ,успешно выполнена" 