telegram job:
  stage: telegram
  script:
  - curl -s -X POST https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage -d "chat_id=$TELEGRAM_GROUP_ID" -d text="Проект ${CI_PROJECT_NAME},  ветки ${CI_COMMIT_BRANCH} ,успешно задиплоян!✅"
  only:
    - master