deploy job:
  stage: deploy
  before_script:
    - docker login -u gitlab-ci-token -p ${CI_JOB_TOKEN} ${CI_REGISTRY}
  script:
    - rm /var/www/docker-compose.yml
    - cp ./docker-compose.yml /var/www
    - cd /var/www
    - docker-compose pull 
    - docker-compose stop  www_django_1|| true #&& docker-compose rm ${CI_REGISTRY_IMAGE} || true
    - docker-compose up -d --force-recreate
  only:
    - master
  tags:
    - shell